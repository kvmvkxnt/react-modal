import './App.scss';
import React from 'react';
import ReactDOM from 'react-dom';

function App() {
    return (
        <main>
            <button className="modal__button">Open with useState</button>
            <button className="modal__button">Open with useRef</button>
            <select className='modal__button'>
                
            </select>
        </main>
    )
}

export default App;
